' -----------------------------------------
' VBA API_Calls:
' Other useful Windows APIs — timers, hooks, system info
' -----------------------------------------
'
' Timers:
' - SetTimer, KillTimer for precise timing beyond Application.OnTime.
'
' Keyboard/mouse hooks:
' - SetWindowsHookEx to intercept input globally or per app.
'
' System information:
' - GetSystemMetrics, GlobalMemoryStatusEx, GetVersionEx for system data.
'
' Clipboard and drag/drop:
' - Advanced clipboard and drag-drop support.
'
' Considerations:
' - Complex declarations and callbacks.
' - Can cause crashes if mishandled.
' - Use only when necessary beyond VBA’s native features.
'
' -----------------------------------------
